PORT=3000
MAX_OTP_REQUESTS_PER_HOUR=3
OTP_EXPIRY_SECONDS=30
RESEND_WINDOW_MINUTES=5
MAX_RESENDS_PER_OTP=3

# Optional SMTP settings.
# If these are not filled in, the project uses development mode and logs the OTP in the terminal.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=
USE_TEMP_STORAGE=false